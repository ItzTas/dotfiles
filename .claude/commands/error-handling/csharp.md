# C# error handling

- **ErrorOr**, **CSharpFunctionalExtensions** (`Result`), **OneOf**, **FluentResults**.
- **LanguageExt** if the team wants full FP.

Preserve the cause with inner exceptions (`new X(msg, err)`).
