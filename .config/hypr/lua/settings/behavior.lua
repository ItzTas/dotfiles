hl.config({
    opengl = {
        nvidia_anti_flicker = false,
    },

    dwindle = {
        preserve_split = true, -- you probably want this
    },

    master = {
        new_status = "master",
        new_on_top = true,
    },

    render = {
        cm_auto_hdr = 2,
    },
})
