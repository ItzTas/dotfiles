# vim: filetype=conf
#
# Plugin bundle file read by antidote (see the "Plugins" section of zshrc).
# Order matters: each plugin's settings file is listed right before the plugin
# itself, and zsh-syntax-highlighting stays last because it wraps ZLE widgets.
#
# Local paths are sourced as-is; user/repo entries are cloned into
# $ANTIDOTE_HOME (${XDG_CACHE_HOME:-$HOME/.cache}/antidote) on first run.
# Update every clone with `update_zsh_plugins` (config/functions).

$ZDOTDIR/plugins/settings/zsh-autosuggestions/settings
zsh-users/zsh-autosuggestions

zsh-users/zsh-completions kind:fpath path:src

$ZDOTDIR/plugins/settings/zsh-syntax-highlighting/settings
zsh-users/zsh-syntax-highlighting
