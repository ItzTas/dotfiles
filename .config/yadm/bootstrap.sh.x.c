#if 0
	shc Version 4.0.3, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -f bootstrap.sh 
#endif

static  char data [] = 
#define      msg1_z	65
#define      msg1	((&data[9]))
	"\262\221\061\270\351\002\373\027\031\150\352\335\143\151\153\132"
	"\071\171\376\355\353\134\231\071\262\210\236\071\163\263\301\275"
	"\257\021\055\152\301\031\177\011\356\116\062\366\233\077\300\272"
	"\100\336\226\364\142\164\367\211\264\242\014\337\363\145\052\004"
	"\051\226\320\361\111\376\370\342\312\235\026\313\010\203\116\161"
	"\161\264\373\345\171\221\031"
#define      lsto_z	1
#define      lsto	((&data[87]))
	"\074"
#define      inlo_z	3
#define      inlo	((&data[88]))
	"\244\011\362"
#define      xecc_z	15
#define      xecc	((&data[91]))
	"\226\003\047\222\342\232\011\336\270\170\051\100\125\115\257"
#define      date_z	1
#define      date	((&data[106]))
	"\206"
#define      text_z	693
#define      text	((&data[217]))
	"\310\271\127\337\205\137\143\323\321\324\210\314\271\001\136\323"
	"\162\154\137\274\375\234\037\044\246\064\334\341\002\176\037\313"
	"\070\167\252\275\326\015\221\247\342\031\164\233\033\322\156\215"
	"\077\316\112\074\153\152\141\021\236\075\363\240\274\023\153\365"
	"\212\026\263\141\044\104\011\006\136\175\242\172\117\021\007\216"
	"\337\122\313\113\274\054\135\132\152\121\373\046\144\147\034\357"
	"\175\317\120\242\024\131\250\162\326\112\355\046\134\364\255\302"
	"\121\116\101\356\252\374\102\022\222\072\105\036\344\124\330\130"
	"\074\317\174\137\014\165\241\312\364\345\017\200\104\061\223\351"
	"\353\277\357\305\060\047\312\127\077\012\133\362\342\233\265\114"
	"\011\014\300\175\017\011\320\211\244\155\163\036\230\215\254\214"
	"\144\214\316\144\141\046\200\140\234\175\051\357\357\233\271\016"
	"\154\352\336\153\375\224\233\174\322\360\101\303\304\203\121\134"
	"\122\246\370\140\101\227\274\011\274\117\170\135\144\273\243\054"
	"\133\067\133\015\011\276\334\104\075\046\325\025\125\123\223\035"
	"\200\033\337\103\163\266\325\041\034\346\022\072\036\335\214\012"
	"\246\104\216\157\140\261\133\351\101\034\246\345\204\003\253\265"
	"\350\154\174\222\126\137\370\160\171\206\145\032\020\132\370\160"
	"\107\341\064\220\024\321\130\344\017\072\360\015\226\002\072\317"
	"\060\256\160\252\036\230\000\345\005\374\072\376\070\247\113\005"
	"\174\021\075\266\140\320\255\002\311\237\060\256\307\372\100\353"
	"\173\336\270\175\136\004\344\372\120\211\346\157\174\364\064\104"
	"\122\245\375\130\165\050\174\015\236\211\127\011\233\132\353\016"
	"\256\113\065\176\107\042\245\174\273\140\266\350\106\254\117\123"
	"\315\170\365\334\103\052\146\131\026\170\314\127\235\270\203\221"
	"\115\225\315\256\363\103\100\251\362\110\063\270\152\131\364\050"
	"\044\025\055\012\121\312\141\054\162\235\243\022\242\303\071\100"
	"\363\045\337\266\162\311\264\012\305\170\152\337\251\234\155\105"
	"\104\102\324\155\065\274\023\216\333\031\052\203\021\257\012\202"
	"\062\236\362\176\010\343\114\117\115\045\266\321\076\135\140\331"
	"\135\111\174\030\306\203\160\223\226\277\120\212\022\047\254\216"
	"\132\320\036\242\244\324\243\121\106\335\142\355\156\234\037\076"
	"\327\217\204\050\120\213\071\244\117\004\267\164\314\115\127\000"
	"\143\365\172\040\066\146\332\134\140\257\025\153\014\317\270\256"
	"\162\156\156\373\071\333\343\221\074\321\051\177\044\144\341\350"
	"\211\073\367\363\143\356\346\203\024\113\022\263\230\322\115\324"
	"\001\006\161\247\036\170\336\276\033\313\307\000\241\036\243\357"
	"\021\206\055\355\363\372\301\370\007\273\000\310\025\201\325\016"
	"\202\146\164\112\034\154\167\065\227\333\317\142\362\156\255\077"
	"\303\037\157\231\147\356\104\250\011\166\270\111\360\124\267\163"
	"\040\104\050\117\154\043\040\044\033\037\104\375\300\352\023\171"
	"\244\257\155\161\064\235\315\076\347\346\041\176\020\125\317\027"
	"\140\155\253\336\122\046\205\220\225\304\174\001\370\015\345\364"
	"\353\013\215\072\040\033\167\001\172\044\171\061\044\130\331\332"
	"\337\022\153\340\156\033\307\233\006\024\313\127\122\016\262\040"
	"\042\355\024\322\105\362\073\114\172\073\370\070\127\216\173\352"
	"\035\361\310\072\314\202\257\155\236\343\313\152\234\165\145\126"
	"\205\047\062\113\341\016\027\056\231\351\227\026\133\102\121\373"
	"\146\011\374\255\272\315\114\224\203\362\232\052\341\335\254\220"
	"\271\163\102\323\235\314\247\025\052\353\234\146\307\017\344\131"
	"\117\114\332\264\074\106\200\207\002\254\344\135\026\066\131\075"
	"\232\300\131\211\076\050\332\340\074\063\210\257\012\323\234\060"
	"\060\221\345\154\330\145\364\333\022\331\070\050"
#define      pswd_z	256
#define      pswd	((&data[988]))
	"\252\122\277\064\220\350\016\161\044\102\372\324\115\316\161\176"
	"\376\002\143\152\332\311\137\266\334\070\356\005\107\200\153\362"
	"\323\053\046\144\023\310\233\104\276\070\055\342\136\345\064\177"
	"\125\312\257\052\154\271\011\223\006\166\352\070\127\105\111\024"
	"\076\310\305\375\221\140\101\120\231\157\063\367\124\147\167\252"
	"\062\046\324\236\337\336\061\346\125\034\036\255\141\147\301\240"
	"\060\207\236\301\347\337\022\200\117\105\170\244\255\357\116\337"
	"\025\043\176\365\001\257\333\127\314\371\004\056\141\306\317\221"
	"\115\155\123\065\115\146\266\234\253\056\101\131\035\217\070\062"
	"\262\266\047\264\146\002\013\063\374\020\141\135\327\061\357\044"
	"\236\103\132\354\251\020\211\124\076\312\255\133\131\346\216\014"
	"\235\265\300\003\270\314\067\265\335\230\023\264\312\002\331\151"
	"\105\063\125\356\103\336\103\202\250\361\335\002\330\154\017\165"
	"\042\317\171\333\234\260\220\171\111\243\055\023\246\006\174\353"
	"\072\322\332\175\261\035\377\132\017\335\134\347\111\154\134\153"
	"\074\325\106\330\205\327\122\317\172\177\342\040\206\137\014\300"
	"\061\346\076\343\004\076\075\024\033\232\373\145\006\130\320\102"
	"\055\027\033\263\356\155\202\151\355\145\211\163\305\226\064\367"
	"\175\162\332\201\260\064\325\070\167\317\015\304\235\176\102\233"
	"\200\246\006\133\160\145\022\114\236\001\121\346\201\275"
#define      tst2_z	19
#define      tst2	((&data[1270]))
	"\376\046\146\242\032\270\066\133\353\233\012\360\235\316\014\334"
	"\273\300\330\332\271\374\063\216"
#define      shll_z	10
#define      shll	((&data[1295]))
	"\102\157\334\041\305\347\307\366\104\060\155\373\373"
#define      opts_z	1
#define      opts	((&data[1306]))
	"\055"
#define      tst1_z	22
#define      tst1	((&data[1309]))
	"\235\310\271\202\326\341\276\005\306\324\122\203\054\151\034\116"
	"\204\062\107\107\010\311\323\152\004\257\025"
#define      msg2_z	19
#define      msg2	((&data[1337]))
	"\210\062\044\103\343\114\243\110\051\135\166\320\105\207\352\322"
	"\145\342\061\341\212\067\140\207"
#define      chk1_z	22
#define      chk1	((&data[1360]))
	"\010\222\014\066\232\301\205\164\012\032\001\036\332\103\126\335"
	"\330\315\021\125\062\226\023\315\320"
#define      rlax_z	1
#define      rlax	((&data[1383]))
	"\062"
#define      chk2_z	19
#define      chk2	((&data[1387]))
	"\100\137\301\272\262\336\111\342\334\370\210\360\360\075\027\376"
	"\117\155\116\371\222\377"/* End of data[] */;
#define      hide_z	4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC	0	/* Define as 1 to debug execvp calls */
#define TRACEABLE	1	/* Define as 1 to enable ptrace the executable */
#define HARDENING	0	/* Define as 1 to disable ptrace/dump the executable */
#define BUSYBOXON	0	/* Define as 1 to enable work with busybox */

#if HARDENING
static const char * shc_x[] = {
"/*",
" * Copyright 2019 - Intika <intika@librefox.org>",
" * Replace ******** with secret read from fd 21",
" * Also change arguments location of sub commands (sh script commands)",
" * gcc -Wall -fpic -shared -o shc_secret.so shc_secret.c -ldl",
" */",
"",
"#define _GNU_SOURCE /* needed to get RTLD_NEXT defined in dlfcn.h */",
"#define PLACEHOLDER \"********\"",
"#include <dlfcn.h>",
"#include <stdlib.h>",
"#include <string.h>",
"#include <unistd.h>",
"#include <stdio.h>",
"#include <signal.h>",
"",
"static char secret[128000]; //max size",
"typedef int (*pfi)(int, char **, char **);",
"static pfi real_main;",
"",
"// copy argv to new location",
"char **copyargs(int argc, char** argv){",
"    char **newargv = malloc((argc+1)*sizeof(*argv));",
"    char *from,*to;",
"    int i,len;",
"",
"    for(i = 0; i<argc; i++){",
"        from = argv[i];",
"        len = strlen(from)+1;",
"        to = malloc(len);",
"        memcpy(to,from,len);",
"        // zap old argv space",
"        memset(from,'\\0',len);",
"        newargv[i] = to;",
"        argv[i] = 0;",
"    }",
"    newargv[argc] = 0;",
"    return newargv;",
"}",
"",
"static int mymain(int argc, char** argv, char** env) {",
"    //fprintf(stderr, \"Inject main argc = %d\\n\", argc);",
"    return real_main(argc, copyargs(argc,argv), env);",
"}",
"",
"int __libc_start_main(int (*main) (int, char**, char**),",
"                      int argc,",
"                      char **argv,",
"                      void (*init) (void),",
"                      void (*fini)(void),",
"                      void (*rtld_fini)(void),",
"                      void (*stack_end)){",
"    static int (*real___libc_start_main)() = NULL;",
"    int n;",
"",
"    if (!real___libc_start_main) {",
"        real___libc_start_main = dlsym(RTLD_NEXT, \"__libc_start_main\");",
"        if (!real___libc_start_main) abort();",
"    }",
"",
"    n = read(21, secret, sizeof(secret));",
"    if (n > 0) {",
"      int i;",
"",
"    if (secret[n - 1] == '\\n') secret[--n] = '\\0';",
"    for (i = 1; i < argc; i++)",
"        if (strcmp(argv[i], PLACEHOLDER) == 0)",
"          argv[i] = secret;",
"    }",
"",
"    real_main = main;",
"",
"    return real___libc_start_main(mymain, argc, argv, init, fini, rtld_fini, stack_end);",
"}",
"",
0};
#endif /* HARDENING */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte. 
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do {
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once. 
 */
void key(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		do {
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data. 
 */
void arc4(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall) \
    BPF_JUMP(BPF_JMP+BPF_JEQ+BPF_K, SYS_##syscall, 0, 1), \
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
    /* validate arch */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, ArchField),
    BPF_JUMP( BPF_JMP+BPF_JEQ+BPF_K, AUDIT_ARCH_X86_64, 1, 0),
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),

    /* load syscall */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, offsetof(struct seccomp_data, nr)),

    /* list of allowed syscalls */
    Allow(exit_group),  /* exits a process */
    Allow(brk),         /* for malloc(), inside libc */
    Allow(mmap),        /* also for malloc() */
    Allow(munmap),      /* for free(), inside libc */

    /* and if we don't match above, die */
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
    .len = sizeof(filter)/sizeof(filter[0]),
    .filter = filter
};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening() {
    if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)) {
        perror("Could not start seccomp:");
        exit(1);
    }
    if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1) {
        perror("Could not start seccomp:");
        exit(1);
    }
} 
/* End Seccomp Sandboxing Init */

void shc_x_file() {
    FILE *fp;
    int line = 0;

    if ((fp = fopen("/tmp/shc_x.c", "w")) == NULL ) {exit(1); exit(1);}
    for (line = 0; shc_x[line]; line++)	fprintf(fp, "%s\n", shc_x[line]);
    fflush(fp);fclose(fp);
}

int make() {
	char * cc, * cflags, * ldflags;
    char cmd[4096];

	cc = getenv("CC");
	if (!cc) cc = "cc";

	sprintf(cmd, "%s %s -o %s %s", cc, "-Wall -fpic -shared", "/tmp/shc_x.so", "/tmp/shc_x.c -ldl");
	if (system(cmd)) {remove("/tmp/shc_x.c"); return -1;}
	remove("/tmp/shc_x.c"); return 0;
}

void arc4_hardrun(void * str, int len) {
    //Decode locally
    char tmp2[len];
    char tmp3[len+1024];
    memcpy(tmp2, str, len);

	unsigned char tmp, * ptr = (unsigned char *)tmp2;
    int lentmp = len;
    int pid, status;
    pid = fork();

    shc_x_file();
    if (make()) {exit(1);}

    setenv("LD_PRELOAD","/tmp/shc_x.so",1);

    if(pid==0) {

        //Start tracing to protect from dump & trace
        if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0) {
            kill(getpid(), SIGKILL);
            _exit(1);
        }

        //Decode Bash
        while (len > 0) {
            indx++;
            tmp = stte[indx];
            jndx += tmp;
            stte[indx] = stte[jndx];
            stte[jndx] = tmp;
            tmp += stte[indx];
            *ptr ^= stte[tmp];
            ptr++;
            len--;
        }

        //Do the magic
        sprintf(tmp3, "%s %s", "'********' 21<<<", tmp2);

        //Exec bash script //fork execl with 'sh -c'
        system(tmp2);

        //Empty script variable
        memcpy(tmp2, str, lentmp);

        //Clean temp
        remove("/tmp/shc_x.so");

        //Sinal to detach ptrace
        ptrace(PTRACE_DETACH, 0, 0, 0);
        exit(0);
    }
    else {wait(&status);}

    /* Seccomp Sandboxing - Start */
    seccomp_hardening();

    exit(0);
}
#endif /* HARDENING */

/*
 * Key with file invariants. 
 */
int key_with_file(char * file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char * sh11, int argc, char ** argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv) {
		fprintf(stderr, "argv=<null>\n");
	} else { 
		for (i = 0; i <= argc ; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char ** argv, char * arg)
{
	for (; argv && *argv && *argv != arg; argv++);
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char * string;
	extern char ** environ;

	mask = (unsigned long)getpid();
	stte_0();
	 key(&chkenv, (void*)&chkenv_end - (void*)&chkenv);
	 key(&data, sizeof(data));
	 key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string) {
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask) {
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void){}

#if HARDENING

static void gets_process_name(const pid_t pid, char * name) {
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE* f = fopen(procfile, "r");
	if (f) {
		size_t size;
		size = fread(name, sizeof (char), sizeof (procfile), f);
		if (size > 0) {
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening() {
    prctl(PR_SET_DUMPABLE, 0);
    prctl(PR_SET_PTRACER, -1);

    int pid = getppid();
    char name[256] = {0};
    gets_process_name(pid, name);

    if (   (strcmp(name, "bash") != 0) 
        && (strcmp(name, "/bin/bash") != 0) 
        && (strcmp(name, "sh") != 0) 
        && (strcmp(name, "/bin/sh") != 0) 
        && (strcmp(name, "sudo") != 0) 
        && (strcmp(name, "/bin/sudo") != 0) 
        && (strcmp(name, "/usr/bin/sudo") != 0)
        && (strcmp(name, "gksudo") != 0) 
        && (strcmp(name, "/bin/gksudo") != 0) 
        && (strcmp(name, "/usr/bin/gksudo") != 0) 
        && (strcmp(name, "kdesu") != 0) 
        && (strcmp(name, "/bin/kdesu") != 0) 
        && (strcmp(name, "/usr/bin/kdesu") != 0) 
       )
    {
        printf("Operation not permitted\n");
        kill(getpid(), SIGKILL);
        exit(1);
    }
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
   #if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
       #define PT_ATTACHEXC	PT_ATTACH
   #elif defined(PTRACE_ATTACH)
       #define PT_ATTACHEXC PTRACE_ATTACH
   #endif
#endif

void untraceable(char * argv0)
{
	char proc[80];
	int pid, mine;

	switch(pid = fork()) {
	case  0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as",  (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR|O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine) {
			kill(pid, SIGCONT);
		} else {
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char * xsh(int argc, char ** argv)
{
	char * scrpt;
	int ret, i, j;
	char ** varg;
	char * me = argv[0];
	if (me == NULL) { me = getenv("_"); }
	if (me == 0) { fprintf(stderr, "E: neither argv[0] nor $_ works."); exit(1); }

	ret = chkenv(argc);
	stte_0();
	 key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date)<time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	 key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret) {
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
	    arc4_hardrun(text, text_z);
	    exit(0);
       /* Seccomp Sandboxing - Start */
       seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		 key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int) ' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	} else {			/* Reexecute */
		if (*xecc) {
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		} else {
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0];		/* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts;	/* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo;	/* Option introducing inline code */
	varg[j++] = scrpt;		/* The script itself */
	if (*lsto)
		varg[j++] = lsto;	/* Option meaning last option */
	i = (ret > 1) ? ret : 0;	/* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++];	/* Main run-time arguments */
	varg[j] = 0;			/* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char ** argv)
{
#if SETUID
   setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
		errno ? ": " : "",
		errno ? strerror(errno) : "",
		argv[1] ? argv[1] : "<null>"
	);
	return 1;
}
