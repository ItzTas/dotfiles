#if 0
	shc Version 4.0.3, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -f ./bootstrap.sh 
#endif

static  char data [] = 
#define      opts_z	1
#define      opts	((&data[0]))
	"\172"
#define      rlax_z	1
#define      rlax	((&data[1]))
	"\005"
#define      date_z	1
#define      date	((&data[2]))
	"\002"
#define      msg2_z	19
#define      msg2	((&data[7]))
	"\264\102\364\247\261\020\342\172\257\127\170\334\101\250\374\017"
	"\236\231\056\267\361\202\015\013\366"
#define      text_z	693
#define      text	((&data[184]))
	"\366\160\156\112\237\314\206\233\165\011\143\037\131\033\160\053"
	"\077\300\067\336\270\241\223\372\226\072\006\214\332\354\071\320"
	"\135\250\033\374\165\241\227\352\253\373\011\004\026\171\060\126"
	"\071\147\064\362\011\307\355\240\002\364\054\335\340\146\256\075"
	"\017\311\072\204\153\322\156\026\315\170\033\343\361\113\071\053"
	"\263\156\036\275\066\014\135\071\000\212\026\340\361\304\036\000"
	"\216\130\205\371\053\364\020\370\154\053\334\135\167\026\211\053"
	"\204\247\350\272\264\106\363\264\320\012\224\302\317\263\303\136"
	"\014\110\127\067\074\150\060\250\224\015\006\014\043\220\067\250"
	"\070\037\143\354\145\127\240\066\141\065\370\060\247\336\153\024"
	"\261\205\045\132\313\012\373\377\051\275\202\334\360\034\237\340"
	"\037\073\140\035\212\061\006\365\231\256\250\261\215\310\156\367"
	"\110\157\105\336\252\314\341\270\020\205\247\361\033\103\256\255"
	"\004\154\060\177\037\206\177\026\310\352\062\364\075\162\173\076"
	"\371\251\147\263\065\365\326\060\017\242\116\165\370\010\364\017"
	"\003\245\117\205\025\056\233\035\123\311\151\252\170\140\172\050"
	"\202\315\135\123\325\053\036\214\262\153\255\261\163\011\320\034"
	"\070\210\210\375\200\107\306\145\154\205\071\015\231\174\011\255"
	"\031\131\164\261\314\324\313\364\260\166\342\002\373\333\373\071"
	"\036\304\165\341\325\362\353\220\226\251\200\224\334\266\205\075"
	"\350\202\072\001\255\146\354\056\162\232\206\013\212\344\267\064"
	"\237\036\171\314\035\137\055\242\000\274\253\330\126\255\353\025"
	"\156\073\327\046\354\202\300\170\057\212\225\372\315\300\340\205"
	"\371\105\334\352\005\355\375\072\014\031\305\174\030\101\315\313"
	"\205\145\056\371\304\162\031\230\021\275\043\313\054\243\221\344"
	"\375\355\365\024\130\030\334\337\152\363\076\170\243\277\374\207"
	"\261\206\042\201\244\215\016\103\240\375\126\233\332\133\323\234"
	"\367\145\307\130\255\020\116\376\207\261\211\046\101\366\242\240"
	"\120\144\222\233\133\332\377\312\044\135\275\050\216\220\054\307"
	"\323\106\342\315\340\032\037\242\242\076\022\041\325\152\356\230"
	"\276\132\354\337\271\174\324\241\341\152\240\130\032\360\235\254"
	"\105\324\156\111\260\200\115\151\112\212\172\062\124\160\146\157"
	"\276\352\006\055\054\216\210\247\100\334\307\067\161\073\113\351"
	"\015\015\103\271\027\043\224\035\214\003\000\214\140\163\227\302"
	"\354\301\110\002\130\254\253\132\055\375\374\116\064\016\322\073"
	"\062\263\012\373\365\142\065\173\027\103\116\074\116\217\000\274"
	"\030\276\114\374\222\176\162\227\002\044\100\150\201\010\077\121"
	"\251\172\306\003\305\302\030\014\241\267\056\037\105\220\234\147"
	"\025\255\355\207\207\045\200\016\272\170\115\145\354\355\300\331"
	"\322\276\121\350\320\011\045\223\311\111\011\267\131\333\024\006"
	"\023\305\042\322\046\303\025\313\002\017\124\011\062\260\276\047"
	"\253\161\003\055\235\011\340\176\321\265\350\066\214\030\105\366"
	"\072\244\217\261\123\251\147\236\247\031\100\341\233\142\250\301"
	"\013\105\102\110\241\045\321\272\324\200\343\261\024\341\253\023"
	"\316\242\330\041\365\212\221\352\124\045\077\263\212\016\350\014"
	"\131\345\106\272\010\117\136\347\364\205\120\217\332\014\271\223"
	"\161\215\012\325\166\040\262\007\114\047\127\332\123\242\343\170"
	"\052\177\242\350\275\307\152\167\217\354\307\266\036\110\265\274"
	"\035\215\173\155\116\110\113\012\035\310\106\121\346\133\365\301"
	"\301\206\031\302\115\346\124\140\047\264\366\057\220\025\340\062"
	"\145\161\325\227\157\375\176\363\203\361\123\331\364\036\301\254"
	"\251\054\127\341\256\032\167\121\177\372\337\362\036\034\324\347"
	"\306\273\113\220\225\370\373\115\173\165\361\322\041\223\262\264"
	"\202\351\273\216\365\004\346\055\100\117\136\351\343\153\360\357"
	"\217\200\046\067\270\106\232\245\254\361\105\343\122\173\333\203"
	"\144\227\022\132\233\371\210\334\110\346\306\053\122\266\033\341"
	"\067\101\030\360\210\262\225\064\244\333\027\367\126\363\173\273"
	"\213\215\025\046\207\235\003\317\204\311\373\326\200\026\267\267"
	"\130\320\247\341\203\074\025\047\030\055\037\156\041\232\052\254"
	"\047\077\323\256\335\327\176\142\240\171\071\040\220\361\327\351"
	"\301\177\312\104\274\340"
#define      lsto_z	1
#define      lsto	((&data[994]))
	"\336"
#define      chk1_z	22
#define      chk1	((&data[996]))
	"\045\106\253\002\337\053\132\264\244\212\346\015\135\006\111\006"
	"\364\164\145\006\233\132\006\155"
#define      tst2_z	19
#define      tst2	((&data[1020]))
	"\257\160\005\314\242\111\312\231\212\255\011\364\255\372\076\054"
	"\217\057\267\307\374\212\207"
#define      tst1_z	22
#define      tst1	((&data[1047]))
	"\365\046\110\205\027\233\033\264\313\036\044\152\257\032\247\170"
	"\227\265\145\224\115\201\214\373\353\025\371"
#define      msg1_z	65
#define      msg1	((&data[1076]))
	"\237\071\035\134\031\212\060\132\142\352\213\204\230\260\313\354"
	"\315\065\252\307\143\215\254\164\106\243\031\366\206\310\023\260"
	"\330\032\003\030\047\237\011\174\164\011\064\031\302\271\176\111"
	"\266\164\117\330\020\143\367\124\070\353\115\316\225\331\230\270"
	"\163\241\007\072\164\163\133\247\047\026\163\127\074\340\063\212"
	"\216\343\207\031\152\002"
#define      pswd_z	256
#define      pswd	((&data[1192]))
	"\054\332\174\104\373\353\035\233\045\072\367\077\305\047\146\334"
	"\233\276\030\174\362\243\012\325\052\043\100\054\051\322\043\126"
	"\255\240\232\251\214\102\114\313\027\017\364\073\326\030\101\001"
	"\156\053\107\112\066\062\343\065\275\203\042\160\244\267\016\143"
	"\161\255\362\225\357\076\140\006\116\125\102\045\156\204\046\334"
	"\260\156\047\346\240\012\033\135\216\075\315\062\364\334\226\146"
	"\211\210\373\171\307\134\200\026\261\303\073\037\107\142\374\367"
	"\320\043\336\160\055\371\315\274\067\233\356\054\167\204\222\001"
	"\014\216\173\324\352\373\352\233\277\045\273\006\210\267\376\130"
	"\333\335\310\011\326\226\305\016\061\263\073\251\067\315\253\104"
	"\133\047\031\106\043\003\341\342\051\234\351\261\124\347\011\057"
	"\304\322\070\233\151\376\251\233\261\344\105\351\262\360\056\016"
	"\030\107\124\073\113\066\035\164\323\007\046\047\356\060\127\263"
	"\002\220\117\154\216\371\007\100\336\114\051\220\075\130\237\125"
	"\237\363\221\352\052\257\137\375\266\205\044\245\265\174\131\270"
	"\015\250\044\234\241\053\334\177\167\005\020\265\136\260\012\375"
	"\243\233\350\315\113\107\313\001\314\360\246\201\154\377\072\172"
	"\247\136\026\111\211\362\311\001\370\332\266\126\212\301\124\056"
	"\135\074\374\250\203\267\104\262\362\073\361\270\142\130\224\376"
	"\026\255\172\010\120\204\336\172\250\036\247\321\361\313\050\237"
	"\154\302\110\371\171\214\253\153\310\234\043\053\364\270\051\013"
	"\145\243\024\266\050\362\061\320\021\330\242\003\244\312\242\020"
	"\214\353\011"
#define      shll_z	10
#define      shll	((&data[1511]))
	"\162\354\366\261\234\311\222\207\377\105\134\100\121"
#define      chk2_z	19
#define      chk2	((&data[1525]))
	"\116\225\034\200\222\136\332\213\202\074\073\322\066\266\035\304"
	"\205\033\352\213\212\121"
#define      inlo_z	3
#define      inlo	((&data[1545]))
	"\357\210\124"
#define      xecc_z	15
#define      xecc	((&data[1549]))
	"\233\301\114\036\061\227\200\240\124\337\151\152\001\317\232\245"
	"\063"/* End of data[] */;
#define      hide_z	4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC	0	/* Define as 1 to debug execvp calls */
#define TRACEABLE	1	/* Define as 1 to enable ptrace the executable */
#define HARDENING	0	/* Define as 1 to disable ptrace/dump the executable */
#define BUSYBOXON	0	/* Define as 1 to enable work with busybox */

#if HARDENING
static const char * shc_x[] = {
"/*",
" * Copyright 2019 - Intika <intika@librefox.org>",
" * Replace ******** with secret read from fd 21",
" * Also change arguments location of sub commands (sh script commands)",
" * gcc -Wall -fpic -shared -o shc_secret.so shc_secret.c -ldl",
" */",
"",
"#define _GNU_SOURCE /* needed to get RTLD_NEXT defined in dlfcn.h */",
"#define PLACEHOLDER \"********\"",
"#include <dlfcn.h>",
"#include <stdlib.h>",
"#include <string.h>",
"#include <unistd.h>",
"#include <stdio.h>",
"#include <signal.h>",
"",
"static char secret[128000]; //max size",
"typedef int (*pfi)(int, char **, char **);",
"static pfi real_main;",
"",
"// copy argv to new location",
"char **copyargs(int argc, char** argv){",
"    char **newargv = malloc((argc+1)*sizeof(*argv));",
"    char *from,*to;",
"    int i,len;",
"",
"    for(i = 0; i<argc; i++){",
"        from = argv[i];",
"        len = strlen(from)+1;",
"        to = malloc(len);",
"        memcpy(to,from,len);",
"        // zap old argv space",
"        memset(from,'\\0',len);",
"        newargv[i] = to;",
"        argv[i] = 0;",
"    }",
"    newargv[argc] = 0;",
"    return newargv;",
"}",
"",
"static int mymain(int argc, char** argv, char** env) {",
"    //fprintf(stderr, \"Inject main argc = %d\\n\", argc);",
"    return real_main(argc, copyargs(argc,argv), env);",
"}",
"",
"int __libc_start_main(int (*main) (int, char**, char**),",
"                      int argc,",
"                      char **argv,",
"                      void (*init) (void),",
"                      void (*fini)(void),",
"                      void (*rtld_fini)(void),",
"                      void (*stack_end)){",
"    static int (*real___libc_start_main)() = NULL;",
"    int n;",
"",
"    if (!real___libc_start_main) {",
"        real___libc_start_main = dlsym(RTLD_NEXT, \"__libc_start_main\");",
"        if (!real___libc_start_main) abort();",
"    }",
"",
"    n = read(21, secret, sizeof(secret));",
"    if (n > 0) {",
"      int i;",
"",
"    if (secret[n - 1] == '\\n') secret[--n] = '\\0';",
"    for (i = 1; i < argc; i++)",
"        if (strcmp(argv[i], PLACEHOLDER) == 0)",
"          argv[i] = secret;",
"    }",
"",
"    real_main = main;",
"",
"    return real___libc_start_main(mymain, argc, argv, init, fini, rtld_fini, stack_end);",
"}",
"",
0};
#endif /* HARDENING */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte. 
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do {
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once. 
 */
void key(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		do {
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data. 
 */
void arc4(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall) \
    BPF_JUMP(BPF_JMP+BPF_JEQ+BPF_K, SYS_##syscall, 0, 1), \
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
    /* validate arch */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, ArchField),
    BPF_JUMP( BPF_JMP+BPF_JEQ+BPF_K, AUDIT_ARCH_X86_64, 1, 0),
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),

    /* load syscall */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, offsetof(struct seccomp_data, nr)),

    /* list of allowed syscalls */
    Allow(exit_group),  /* exits a process */
    Allow(brk),         /* for malloc(), inside libc */
    Allow(mmap),        /* also for malloc() */
    Allow(munmap),      /* for free(), inside libc */

    /* and if we don't match above, die */
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
    .len = sizeof(filter)/sizeof(filter[0]),
    .filter = filter
};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening() {
    if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)) {
        perror("Could not start seccomp:");
        exit(1);
    }
    if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1) {
        perror("Could not start seccomp:");
        exit(1);
    }
} 
/* End Seccomp Sandboxing Init */

void shc_x_file() {
    FILE *fp;
    int line = 0;

    if ((fp = fopen("/tmp/shc_x.c", "w")) == NULL ) {exit(1); exit(1);}
    for (line = 0; shc_x[line]; line++)	fprintf(fp, "%s\n", shc_x[line]);
    fflush(fp);fclose(fp);
}

int make() {
	char * cc, * cflags, * ldflags;
    char cmd[4096];

	cc = getenv("CC");
	if (!cc) cc = "cc";

	sprintf(cmd, "%s %s -o %s %s", cc, "-Wall -fpic -shared", "/tmp/shc_x.so", "/tmp/shc_x.c -ldl");
	if (system(cmd)) {remove("/tmp/shc_x.c"); return -1;}
	remove("/tmp/shc_x.c"); return 0;
}

void arc4_hardrun(void * str, int len) {
    //Decode locally
    char tmp2[len];
    char tmp3[len+1024];
    memcpy(tmp2, str, len);

	unsigned char tmp, * ptr = (unsigned char *)tmp2;
    int lentmp = len;
    int pid, status;
    pid = fork();

    shc_x_file();
    if (make()) {exit(1);}

    setenv("LD_PRELOAD","/tmp/shc_x.so",1);

    if(pid==0) {

        //Start tracing to protect from dump & trace
        if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0) {
            kill(getpid(), SIGKILL);
            _exit(1);
        }

        //Decode Bash
        while (len > 0) {
            indx++;
            tmp = stte[indx];
            jndx += tmp;
            stte[indx] = stte[jndx];
            stte[jndx] = tmp;
            tmp += stte[indx];
            *ptr ^= stte[tmp];
            ptr++;
            len--;
        }

        //Do the magic
        sprintf(tmp3, "%s %s", "'********' 21<<<", tmp2);

        //Exec bash script //fork execl with 'sh -c'
        system(tmp2);

        //Empty script variable
        memcpy(tmp2, str, lentmp);

        //Clean temp
        remove("/tmp/shc_x.so");

        //Sinal to detach ptrace
        ptrace(PTRACE_DETACH, 0, 0, 0);
        exit(0);
    }
    else {wait(&status);}

    /* Seccomp Sandboxing - Start */
    seccomp_hardening();

    exit(0);
}
#endif /* HARDENING */

/*
 * Key with file invariants. 
 */
int key_with_file(char * file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char * sh11, int argc, char ** argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv) {
		fprintf(stderr, "argv=<null>\n");
	} else { 
		for (i = 0; i <= argc ; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char ** argv, char * arg)
{
	for (; argv && *argv && *argv != arg; argv++);
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char * string;
	extern char ** environ;

	mask = (unsigned long)getpid();
	stte_0();
	 key(&chkenv, (void*)&chkenv_end - (void*)&chkenv);
	 key(&data, sizeof(data));
	 key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string) {
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask) {
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void){}

#if HARDENING

static void gets_process_name(const pid_t pid, char * name) {
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE* f = fopen(procfile, "r");
	if (f) {
		size_t size;
		size = fread(name, sizeof (char), sizeof (procfile), f);
		if (size > 0) {
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening() {
    prctl(PR_SET_DUMPABLE, 0);
    prctl(PR_SET_PTRACER, -1);

    int pid = getppid();
    char name[256] = {0};
    gets_process_name(pid, name);

    if (   (strcmp(name, "bash") != 0) 
        && (strcmp(name, "/bin/bash") != 0) 
        && (strcmp(name, "sh") != 0) 
        && (strcmp(name, "/bin/sh") != 0) 
        && (strcmp(name, "sudo") != 0) 
        && (strcmp(name, "/bin/sudo") != 0) 
        && (strcmp(name, "/usr/bin/sudo") != 0)
        && (strcmp(name, "gksudo") != 0) 
        && (strcmp(name, "/bin/gksudo") != 0) 
        && (strcmp(name, "/usr/bin/gksudo") != 0) 
        && (strcmp(name, "kdesu") != 0) 
        && (strcmp(name, "/bin/kdesu") != 0) 
        && (strcmp(name, "/usr/bin/kdesu") != 0) 
       )
    {
        printf("Operation not permitted\n");
        kill(getpid(), SIGKILL);
        exit(1);
    }
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
   #if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
       #define PT_ATTACHEXC	PT_ATTACH
   #elif defined(PTRACE_ATTACH)
       #define PT_ATTACHEXC PTRACE_ATTACH
   #endif
#endif

void untraceable(char * argv0)
{
	char proc[80];
	int pid, mine;

	switch(pid = fork()) {
	case  0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as",  (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR|O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine) {
			kill(pid, SIGCONT);
		} else {
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char * xsh(int argc, char ** argv)
{
	char * scrpt;
	int ret, i, j;
	char ** varg;
	char * me = argv[0];
	if (me == NULL) { me = getenv("_"); }
	if (me == 0) { fprintf(stderr, "E: neither argv[0] nor $_ works."); exit(1); }

	ret = chkenv(argc);
	stte_0();
	 key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date)<time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	 key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret) {
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
	    arc4_hardrun(text, text_z);
	    exit(0);
       /* Seccomp Sandboxing - Start */
       seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		 key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int) ' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	} else {			/* Reexecute */
		if (*xecc) {
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		} else {
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0];		/* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts;	/* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo;	/* Option introducing inline code */
	varg[j++] = scrpt;		/* The script itself */
	if (*lsto)
		varg[j++] = lsto;	/* Option meaning last option */
	i = (ret > 1) ? ret : 0;	/* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++];	/* Main run-time arguments */
	varg[j] = 0;			/* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char ** argv)
{
#if SETUID
   setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
		errno ? ": " : "",
		errno ? strerror(errno) : "",
		argv[1] ? argv[1] : "<null>"
	);
	return 1;
}
