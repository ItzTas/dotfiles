#!/usr/bin/env fish
# ~/.config/fish/config/setopt.fish
#
# fish has no setopt: the shell options zsh sets in config/setopt are either
# fish defaults or have no counterpart, which leaves the greeting.

# Start with an empty screen instead of the welcome message
set -g fish_greeting
